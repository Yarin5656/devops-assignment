# evidence/

Place all proof artifacts here for final submission.

- `screenshots/` — UI screenshots (Jenkins jobs, ingress, resources)
- `logs/` — pipeline logs and command outputs

Keep sensitive values redacted.
