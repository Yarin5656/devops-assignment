# iac/

Infrastructure-as-Code workspace for **local-only** assignment execution.

- Cloud APIs must be simulated via LocalStack only.
- No real AWS endpoints.
- No real cloud credentials.

Terraform scaffolding is under `iac/terraform/`.
